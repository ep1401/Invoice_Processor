You are extracting invoice data from a PDF invoice for downstream tax processing.

Read the uploaded invoice PDF and extract only the fields required by the provided response schema.

Field guidance:

- invoice_id:
  Use the invoice number or invoice identifier shown on the document.
  Use the document's actual invoice number directly.
  Do not invent or infer a new identifier.

- special_notes:
  Extract any invoice-level note related to tax handling or special instructions.
  Examples include statements such as "do not tax", "no tax required", "tax already applied", or similar language.
  If there is no relevant invoice-level note, return null.

- apply_additional_tax:
  Return false only if the invoice clearly indicates that no additional tax should be added.
  This includes statements such as:
  - "do not tax"
  - "no tax required"
  - "tax already applied"
  - "already taxed"
  If there is no clear invoice-level instruction preventing additional tax, return true.

- displayed_total:
  Extract the final total shown on the invoice if it is clearly visible.
  If the final total is not clearly visible, return null.

- line_items:
  Extract all visible invoice line items you can identify from the invoice.

For each line item:

- description:
  Extract the product or service description from the line item.

  Keep information that identifies or distinguishes the item itself, such as product type, variant, size, pack size, strength, model, condition, and other descriptive attributes. Preserve condition terms (e.g., "used", "new", "refurbished") when they appear in the line item text.

  Exclude administrative or row-level metadata that is not part of the product or service description, such as internal identifiers, SKU-like codes, and explicit quantity labels.

  The description should remain faithful to the invoice text while excluding appended metadata that does not describe the item itself.
  
- quantity:
  Extract the quantity shown for the line item.
  If quantity is missing but the row clearly represents a single billed item, return 1.

- unit_price:
  Extract the per-unit price only if it is explicitly shown on the invoice.
  Do not infer, estimate, or calculate it from quantity and line total.
  If it is not explicitly shown, return null.

- line_total:
  Extract the monetary amount associated with that row.

- tax_category:
  Choose exactly one category from the allowed categories listed below.
  Do not invent new categories.
  Always return one allowed category exactly as written.

  When multiple categories could apply, select the most specific applicable category rather than a broader general category.

  Base the category on what the item actually is, not on the broader department or use case it belongs to. Prefer the category that matches the item’s direct product type or physical form.
  
Important rules:

1. Return every visible invoice line item you can identify.
2. Use the invoice number shown on the document as invoice_id.
3. Use special_notes only for invoice-level tax-related notes or special tax instructions.
4. Normalize numeric values as numbers, not strings.
5. If an item description says "used" but there is no invoice-level instruction that tax should not be added, do not assume that alone means no tax.
6. If the invoice clearly says tax is already applied or no tax is required, set apply_additional_tax to false.
7. Use only the allowed tax categories listed below.
8. If a value is unclear or not explicitly shown and the schema allows null, return null rather than guessing, inferring, or calculating it.
9. Do not include reasoning, commentary, or confidence statements in extracted field values.
10. Prefer consistent formatting across all line items within the same invoice.

Allowed tax categories:
{allowed_categories}

The uploaded file name is:
{file_name}